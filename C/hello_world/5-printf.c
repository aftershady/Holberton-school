/**
*main - return nothing
*Description: 'display a string in a printf'
*Return: Always 0 (Success)
*/
#include <stdio.h>

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}

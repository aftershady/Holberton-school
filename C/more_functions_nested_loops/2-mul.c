#include <unistd.h>
#include "main.h"
/**
 * mul - multiplicator
 * @a: number
 * @b: number2
 * Description: multiplication of 2 integers
 * Return: the multiplication
 */
int mul(int a, int b)
{
return (a * b);
}
